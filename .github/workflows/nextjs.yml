# The marketing site deploys via docs-pages.yml (static docs/ folder).
# The full Next.js app deploys to Vercel — not GitHub Pages.
#
# This workflow is intentionally disabled. The previous Next.js → Pages export
# could not run API routes and overwrote the static docs site.

name: Deploy Next.js site to Pages (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use docs-pages.yml for GitHub Pages and Vercel for the app."
